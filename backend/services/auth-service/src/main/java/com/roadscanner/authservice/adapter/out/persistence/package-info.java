/**
 * JPA/Postgres implementations of the repository ports declared in domain.port.out.
 *
 * Intentionally empty as of this bootstrap — no JPA entities exist yet (per today's scope,
 * entities/repositories are explicitly excluded). See
 * docs/services/auth-service/database-design.md and implementation-roadmap.md step 4.
 */
package com.roadscanner.authservice.adapter.out.persistence;
