/**
 * Inbound use-case ports: RegisterUser, AuthenticateUser, RefreshAccessToken, RevokeSession,
 * RevokeAllSessions, RequestPasswordReset, ConfirmPasswordReset, AssignRole.
 *
 * Intentionally empty as of this bootstrap — these define the service's behavior and are
 * explicitly out of scope for today's foundation-only bootstrap. See
 * docs/services/auth-service/api-contract.md and implementation-roadmap.md step 3.
 */
package com.roadscanner.authservice.domain.port.in;
