/**
 * Spring Boot wiring: bean configuration (Jackson, CORS, OpenAPI, Redis client, Persistence
 * scanning), the Kafka consumer factories and error handling (KafkaConfig), the
 * inventory-service HTTP client (InventoryClientConfig), operational tuning properties
 * (SearchProperties, InventoryServiceProperties), and explicit use-case/domain-policy wiring
 * (UseCaseConfig).
 */
package com.roadscanner.searchservice.config;
